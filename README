usage: flash-uboot.py [-h] [--mtd] [--mmc MMC] [--get-version GET_VERSION]
                      [--get-file-version GET_FILE_VERSION] [--write]
                      [--verify] [--spl SPL] [--spl-offset SPL_OFFSET]
                      [--uboot UBOOT] [--uboot-offset UBOOT_OFFSET]
                      [--gpio GPIO]

u-boot programmer

optional arguments:
  -h, --help            show this help message and exit
  --mtd                 Flash type mtd. Will search for /proc/mtd partitions
                        "u-boot" and "spl"
  --mmc MMC             Flash type mmc
  --get-version GET_VERSION
                        Read version string from section in flash. Available
                        sections: "uboot", "spl"
  --get-file-version GET_FILE_VERSION
                        Read version string from file
  --write               Write to flash
  --verify              Verify only, ignores write flag
  --spl SPL             SPL binary
  --spl-offset SPL_OFFSET
                        SPL offset in flash
  --uboot UBOOT         u-boot binary
  --uboot-offset UBOOT_OFFSET
                        u-boot offset in flash
  --gpio GPIO           Flash write protect gpio number

Returns 0 for success and 1 for failure
